DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/pagila
OPENAI_API_KEY=sk-replace-me
OPENAI_MODEL=gpt-4o-mini
TRIAGE_CONFIDENCE_THRESHOLD=0.5
# Optional: path to a trusted CA bundle when HTTPS traffic is inspected by a proxy.
# WEB_SEARCH_CA_BUNDLE=/etc/ssl/cert.pem
